/* CESTA — configuração opcional da sincronização (F8).

   ESTE ARQUIVO FICA VAZIO NO REPOSITÓRIO, DE PROPÓSITO. O repo é público, e as
   credenciais são digitadas dentro do app (Ajustes → Sincronização), ficando só
   no localStorage do aparelho. Preencher aqui "para facilitar" publicaria a
   chave para qualquer pessoa que abrisse o GitHub.

   Quem quiser entregar o app já configurado preenche uma cópia local — veja
   js/config.local.js no .gitignore. */
'use strict';

const CONFIG = {
  url: '',
  anonKey: '',
};
