# Copie para .env e preencha. O .env esta no .gitignore — nao vai para o
# repositorio, e nada daqui e enviado para lugar nenhum: quem le e o script
# supabase/verificar.js, rodando na sua maquina.
#
# Projeto: domi-compras (Supabase → Settings → API)
SUPABASE_URL=
SUPABASE_ANON_KEY=
